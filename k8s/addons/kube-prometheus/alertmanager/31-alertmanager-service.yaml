apiVersion: v1
kind: Service
metadata:
  name: alertmanager-main
  namespace: monitoring
  labels:
    alertmanager: main
spec:
  selector:
    alertmanager: main
    app: alertmanager
  ports:
  - name: web
    port: 9093
    targetPort: web
  sessionAffinity: ClientIP
